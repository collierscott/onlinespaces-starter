{% extends 'layouts/full_page_fluid.html.twig' %}
{% block title %}Articles{% endblock %}

{% block body %}
    <div class="container">
        <h1>Articles</h1>
        <a href="{{ path('admin_article_new') }}" class="btn btn-primary">Add New Article</a>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Title
                    </th>
                    <th>
                        Author
                    </th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {% for article in articles %}
                    <tr>
                        <td>{{ article.title }}</td>
                        <td>{{ article.author.username }}</td>
                        <td>
                            <a href="{{ path('admin_article_delete', {
                                id: article.id
                            }) }}">
                                <span class="fa fa-{{ article.isPublished ? 'check' : 'times' }}"></span>
                            </a>
                        </td>
                        <td>
                            <a href="{{ path('admin_article_edit', {
                                id: article.id
                            }) }}">
                                <span class="fa fa-pencil"></span>
                            </a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}