<h3>Hello {{ user.username }}, please confirm your email address.</h3>
<p>
    Please confirm you email address by clicking on
    <a href="{{ url('confirm_user_token', {'token': user.confirmationToken }) }}">this link</a> or use this url:
    <b>{{ url('confirm_user_token', {'token': user.confirmationToken }) }}</b>
</p>
